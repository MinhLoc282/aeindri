import { connect } from 'react-redux';

// import {
//   actionConnectMetaMask,
//   actionAddUser,
//   actionUploadImage,
//   actionLogout,
//   actionGetUser,
// } from 'store/actions';

import Header from './Header';

const mapStateToProps = () => ({
});

const mapDispatchToProps = () => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(Header);
